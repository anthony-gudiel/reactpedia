// App.test.jsx
// Currently, the test cases are placeholders to ensure that the test suite is working properly.

import { expect, test } from 'vitest'

test('adds 1 + 2 to equal 3', () => {
    const num1 = 1;
    const num2 = 2;

    const result = num1 + num2;

    expect(result).toBe(3)
})